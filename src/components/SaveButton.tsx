import { MdCheck } from 'react-icons/md';
import IconButton from '@/components/IconButton';

const SaveButton = () => (
  <IconButton color='green' type='submit'>
    <MdCheck />
  </IconButton>
);

export default SaveButton;
